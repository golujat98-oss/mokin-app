import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartbookingpro.app',
  appName: 'Smart Booking Pro',
  webDir: 'public'
};

export default config;
